# Backend

Java 21 + Spring Boot 3.5 + JPA/QueryDSL + MySQL

상세: `docs/backend.md`, DB/API: `docs/architecture.md`

```bash
./gradlew bootRun        # 실행
./gradlew test           # 테스트
./gradlew compileJava    # Q클래스 생성
```
